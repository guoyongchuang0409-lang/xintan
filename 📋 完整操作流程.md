# 📋 完整操作流程

## 🎯 目标

将代码推送到 GitHub，然后通过 Vercel 部署后端，实现数据库功能。

---

## 🚀 快速流程（5 步完成）

```
第 1 步：推送代码到 GitHub
    ↓
第 2 步：Vercel 导入并部署
    ↓
第 3 步：创建数据库
    ↓
第 4 步：初始化数据库表
    ↓
第 5 步：配置 Flutter 应用
```

---

## 📋 第 1 步：推送代码到 GitHub

### 选择一种方式：

#### 方式 A：使用脚本（最快）⭐

**Windows：**
```bash
cd personality_quiz_app
push_to_github.bat
```

**Mac/Linux：**
```bash
cd personality_quiz_app
chmod +x push_to_github.sh
./push_to_github.sh
```

#### 方式 B：使用 GitHub Desktop（最简单）

1. 下载：https://desktop.github.com/
2. 登录 GitHub
3. File → Add Local Repository → 选择 `personality_quiz_app`
4. Publish repository

#### 方式 C：手动命令

```bash
cd personality_quiz_app
git init
git remote add origin https://github.com/guoyongchuang0409-lang/xintan.git
git add .
git commit -m "添加数据库功能"
git branch -M main
git push -u origin main --force
```

### ✅ 验证

访问：https://github.com/guoyongchuang0409-lang/xintan

应该能看到所有文件。

---

## 📋 第 2 步：Vercel 导入并部署

### 1. 访问 Vercel

https://vercel.com/signup

### 2. 使用 GitHub 登录

点击 **"Continue with GitHub"** ⭐ 必须用 GitHub 登录

### 3. 导入项目

1. 点击 **"Add New..."** → **"Project"**
2. 找到 **"xintan"** 仓库
3. 点击 **"Import"**

### 4. 配置项目（重要！）

- **Root Directory**: 点击 "Edit"，输入 `backend/vercel` ⭐
- **Framework Preset**: 选择 "Other"
- 点击 **"Deploy"**

### 5. 等待部署

等待 30-60 秒，看到 🎉 表示成功

### 6. 记录 API 地址

例如：`https://xintan-abc123.vercel.app`

**保存这个地址！**

---

## 📋 第 3 步：创建数据库

### 1. 进入 Storage

在 Vercel 项目页面，点击 **"Storage"** → **"Create Database"**

### 2. 创建 Postgres

- 选择 **"Postgres"**
- Database Name: `quiz_db`
- Region: **"Singapore"** 或 **"Hong Kong"**
- 点击 **"Create"**

### 3. 连接到项目

- 选择你的项目
- 点击 **"Connect"**

✅ 环境变量自动配置完成！

---

## 📋 第 4 步：初始化数据库表

### 1. 打开 Query

点击数据库 → **"Query"** 标签

### 2. 执行 SQL

复制粘贴以下 SQL：

```sql
CREATE TABLE IF NOT EXISTS reports (
    id SERIAL PRIMARY KEY,
    share_code VARCHAR(10) NOT NULL UNIQUE,
    quiz_type_id VARCHAR(50) NOT NULL,
    quiz_type_name VARCHAR(100) NOT NULL,
    report_data JSONB NOT NULL,
    created_at TIMESTAMP NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMP,
    last_viewed_at TIMESTAMP,
    view_count INTEGER DEFAULT 0,
    ip_address VARCHAR(45),
    CONSTRAINT idx_share_code UNIQUE (share_code)
);

CREATE INDEX idx_quiz_type ON reports(quiz_type_id);
CREATE INDEX idx_created_at ON reports(created_at);
```

点击 **"Run Query"**，看到 Success 即可。

---

## 📋 第 5 步：配置 Flutter 应用

### 1. 修改配置

打开：`lib/core/services/database_service.dart`

第 12 行：
```dart
static const String _apiBaseUrl = 'YOUR_VERCEL_API_URL';
```

改为：
```dart
static const String _apiBaseUrl = 'https://xintan-abc123.vercel.app';
```

### 2. 测试 API

在浏览器访问：
```
https://xintan-abc123.vercel.app/api/stats
```

应该看到 JSON 数据。

---

## 🎉 完成！

现在你的后端已经部署成功！

### 下一步（可选）

1. 重新构建 Flutter：`flutter build web --release`
2. 上传到 Netlify
3. 测试自动上传功能

---

## 📚 详细文档

- **[GitHub推送指南.md](./GitHub推送指南.md)** - 详细的推送步骤
- **[Vercel部署指南.md](./Vercel部署指南.md)** - 详细的部署步骤
- **[网页部署指南.md](./网页部署指南.md)** - 完全网页操作指南

---

## ⏱️ 预计时间

- 第 1 步：2 分钟
- 第 2 步：3 分钟
- 第 3 步：2 分钟
- 第 4 步：1 分钟
- 第 5 步：1 分钟

**总计：约 10 分钟**

---

## 🆘 遇到问题？

### 推送失败

查看：[GitHub推送指南.md](./GitHub推送指南.md) 的常见问题部分

### 部署失败

查看：[Vercel部署指南.md](./Vercel部署指南.md) 的常见问题部分

### API 不工作

1. 检查 Root Directory 是否设置为 `backend/vercel`
2. 检查数据库是否已连接
3. 查看 Vercel 的 Functions 日志

---

## 📞 需要帮助？

告诉我你在哪一步遇到了问题，我会帮你解决！

---

**现在开始第 1 步吧！** 🚀

选择一种方式推送代码到 GitHub，完成后告诉我结果。
